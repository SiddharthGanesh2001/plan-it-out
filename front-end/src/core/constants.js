const FETCH_URL = "http://localhost:8080";

const ConfigHolder = {
    userId: 2
}

const CATEGORIES = [
    "sports",
    "party",
    "concert",
    "conference",
    "workshop",
    "seminar",
    "hackathon",
    "custom"
];

export { FETCH_URL, ConfigHolder, CATEGORIES };
